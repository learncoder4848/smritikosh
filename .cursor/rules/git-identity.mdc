---
description: Git identity and PR author for this repo
alwaysApply: true
---

# Git Identity — smritikosh

Always use the `learncoder4848` GitHub identity for this repository.

## Local git config (already set)

```
user.name  = Shantanu Vashishtha
user.email = vashishthashantanu3@gmail.com
```

## Commits

- Author on every commit must be `Shantanu Vashishtha <learncoder4848@users.noreply.github.com>`
- If amending or squashing, re-apply with `--reset-author`

## Push & PR

```bash
git push origin <branch>

gh pr create \
  --title "<type> | <title>" \
  --body "<1-3 sentence description>" \
  --base main
```

The `gh` CLI is already authenticated as `learncoder4848` on `github.com`.
