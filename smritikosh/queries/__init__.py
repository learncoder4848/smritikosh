"""Tree-sitter tag queries, one `tags.scm` per language directory.

This package exists so `importlib.resources.files("smritikosh.queries")` has an
anchor; the language directories themselves are plain data directories.
"""
